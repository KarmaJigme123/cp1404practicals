"""CP1404 - Practical 03Student name: Karma Jigme Wangchuk"""import random# Constants for maximum increase, maximum decrease, minimum and maximum prices, and initial priceMAX_INCREASE = 0.1  # 10%MAX_DECREASE = 0.05  # 5%MIN_PRICE = 0.01MAX_PRICE = 1000.0INITIAL_PRICE = 10.0# Initialize price with the initial valueprice = INITIAL_PRICE# Display the initial price with proper formattingprint(f"The initial price is: ${price:,.2f}")# Initialize the day countdays = 0while MIN_PRICE <= price <= MAX_PRICE:    price_change = 0    if random.randint(1, 2) == 1:        price_change = random.uniform(0, MAX_INCREASE)    else:        price_change = random.uniform(-MAX_DECREASE, 0)    # Update the price according to the price change    price *= (1 + price_change)    # Increment the day count    days += 1    print(f"On day {days}, the price is: ${price:,.2f}")