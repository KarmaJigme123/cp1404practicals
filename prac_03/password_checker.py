"""CP1404 - Practical 03Student name: Karma Jigme Wangchuk"""MIN_LENGTH = 5MAX_LENGTH = 15SPECIAL_CHARS_REQUIRED = TrueSPECIAL_CHARACTERS = "!@#$%^&*()_-=+`~,./'[]<>?{}|\\"def main():    """Program to prompt and validate a user's password."""    print("Please create a valid password.")    print(f"Your password must be between {MIN_LENGTH} and {MAX_LENGTH} characters long and include:")    print("\t- At least one uppercase letter")    print("\t- At least one lowercase letter")    print("\t- At least one digit")    if SPECIAL_CHARS_REQUIRED:        print(f"\t- At least one special character from: {SPECIAL_CHARACTERS}")    password = input("> ")    while not is_valid_password(password):        print("Invalid password!")        password = input("> ")    print(f"Your password, {len(password)} characters long, is valid: {password}")def is_valid_password(password):    """Check if the provided password meets the criteria."""    if len(password) < MIN_LENGTH or len(password) > MAX_LENGTH:        return False    has_lower = False    has_upper = False    has_digit = False    has_special = False    for char in password:        if char.islower():            has_lower = True        elif char.isupper():            has_upper = True        elif char.isdigit():            has_digit = True        elif char in SPECIAL_CHARACTERS:            has_special = True    if not has_lower or not has_upper or not has_digit or (SPECIAL_CHARS_REQUIRED and not has_special):        return False    return Truemain()